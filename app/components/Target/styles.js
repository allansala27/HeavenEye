import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  content:{
        flex:1,
        alignItems:'center'     //<-----
  }
});
